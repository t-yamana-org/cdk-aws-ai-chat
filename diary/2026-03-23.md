- @aws-amplify/backend の一部(linker) に docker 依存があり amplify の cicd ビルド失敗していると調査
    - 特別なことをしていないアプリで backend のビルドに失敗するのはおかしいため
    - 公式ドキュメントに vite-react のテンプレートから cicd ビルドに成功
    - そちらでも backend パッケージを利用しているがビルドに成功している。違いは未確認。
    - そちらをベースに開発していくことにする
