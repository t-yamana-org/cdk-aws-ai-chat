- git-secrets の導入
    - 自動的に pre-commit フック登録される
    - 下記でブロックしてくれることを確認した
    - [ERROR] Matched one or more prohibited patterns
- pre-commit 設定を追加
    - npm run prepare を実行すると設定する
    - .git/hooks/pre-commit を直接編集して調整することもある
    - git-secrets も .git/hooks/pre-commit で動作するのでケンカしていた
    - git-secrets を lint-staged 配下にすることで設定を simple-git-hooks に一元化できた
